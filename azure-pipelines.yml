
trigger:
- main

pool:
  name: dockeragent

steps:

- powershell: |
    Write-Host "Agent Work Folder"
    dir "C:\Users\91983\Desktop\dockeragent\vsts-agent-win-x64-4.274.1\_work\_tool\terraform" -Recurse
  displayName: 'Check Terraform Installation'

- powershell: |
    Write-Host "Source Directory"
    Write-Host "$(Build.SourcesDirectory)"
    dir "$(Build.SourcesDirectory)" -Recurse
  displayName: 'Check Repository Structure'